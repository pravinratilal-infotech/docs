# Moved to dev/docker-compose.yml
#
# Use the dev toolbox instead:
#   ./dev/tools.sh up        # PdfDing + Postgres + Docling
#   ./dev/tools.sh shell     # dev container
#   ./dev/tools.sh test      # pytest
#   ./dev/tools.sh drive-test
#
# Or from repo root:
#   ./tools.sh up
